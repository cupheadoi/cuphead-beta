import type { Rank } from '../types';
const items:[Rank,string,string,string][]=[['pawn','پیاده','♟','piece-pawn'],['knight','اسب','♞','piece-knight'],['bishop','فیل','♝','piece-bishop'],['rook','رخ','♜','piece-rook'],['queen','وزیر','♛','piece-queen'],['king','شاه','♚','piece-king']];
export default function RankSelector({rank,onChange}:{rank:Rank;onChange:(r:Rank)=>void}){return <div className="mt-6 sm:mt-7 flex flex-wrap justify-center gap-2 sm:gap-3 mb-10">{items.map(([id,label,piece,color])=><button key={id} onClick={()=>onChange(id)} className={`rank-chip ${color} ${rank===id?'selected':''}`}><span className="text-2xl leading-none" aria-hidden="true">{piece}</span><span>{label}</span><span className="hidden sm:inline text-[10px] opacity-60 uppercase tracking-widest">{id}</span></button>)}</div>}
