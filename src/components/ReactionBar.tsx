import { useEffect,useState } from 'react';
import { ThumbsDown,ThumbsUp } from 'lucide-react';
import { api } from '../lib/api';
export default function ReactionBar({contentType,contentId}:{contentType:string;contentId:string}){const [value,setValue]=useState(0),[counts,setCounts]=useState<{likes?:number;dislikes?:number}>({}),[staff,setStaff]=useState(false);useEffect(()=>{let live=true;Promise.all([api.reactions(contentType,contentId),api.me().catch(()=>null)]).then(([r,m])=>{if(!live)return;setValue(r.userValue||0);setCounts(r);setStaff(['owner','admin'].includes(m?.user?.role||''))});return()=>{live=false}},[contentType,contentId]);async function react(next:number){if(!api.me)return;try{const r=await api.react({contentType,contentId,value:next});setValue(next);if(r.likes!==undefined)setCounts(r)}catch{/* anonymous users are simply invited to log in by the API */}}return <div className="flex items-center gap-2 mt-5 pt-4 border-t border-dark-border"><span className="text-xs text-slate-500">نظر شما</span><button onClick={()=>react(1)} className={`reaction-btn ${value===1?'active':''}`} aria-label="پسندیدم"><ThumbsUp size={14}/> پسندیدم</button><button onClick={()=>react(-1)} className={`reaction-btn ${value===-1?'active':''}`} aria-label="نپسندیدم"><ThumbsDown size={14}/> نپسندیدم</button>{staff&&<span className="text-xs text-slate-500 ms-auto">پسندیده: {counts.likes||0} · نپسندیده: {counts.dislikes||0}</span>}</div>}
